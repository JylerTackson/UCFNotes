Create a 1D NumPy array containing the integers from 0 to 9 (inclusive). Then, create a
second array that contains only the even numbers from the first array.

```python
```

---
Given a 2D NumPy array A of shape (3, 4), write code to compute: (1) the mean of each
column, and (2) the sum of each row. Your answer should show both operations.

```python
```

---
You have two NumPy arrays, $x$ and $y$, each of shape (100,). Write NumPy code to
compute the element-wise product of $x$ and $y$, and then the dot product (scalar) of $x$ and
$y$.
```python
```

---

Create a 3-by-3 identity matrix using NumPy. Then, show how to extract the diagonal
elements of any 3-by-3 NumPy array M as a 1D array.

```python
```
